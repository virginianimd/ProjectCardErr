<%= form_with(model: boardmember, local: true) do |form| %>
  <% if boardmember.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boardmember.errors.count, "error") %> prohibited this boardmember from being saved:</h2>

      <ul>
        <% boardmember.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :board_id %>
    <%= form.number_field :board_id %>
  </div>

  <div class="field">
    <%= form.label :role %>
    <%= form.number_field :role %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
